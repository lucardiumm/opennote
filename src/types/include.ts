export type Starred = {
    icon: string;
    name: string;
    from: number;
    to: number;
}

export type UserType = {
    email: string;
    password: string;
    name: string;
    image: string;
}